# 보고서 작성용 연구 노트 (경기도 시·군별 구급차 상시대기 재배치 최적화)

**2026-09-11 전환 안내**: 연구 문제가 "신설 병원 입지"에서 "기존 구급차 상시대기 위치 재배치"로, 대상 도시가 수원시에서 **전주시(전북특별자치도, 52110)**로 바뀌었습니다. 아래 1~2절의 수원·경기도 수치는 방법 설명의 예시로 남기고, 전주 수치는 `docs/jeonju_data_ko.md`에 정리한 뒤 이 문서 5절에 옮깁니다. 아래 0~4절의 데이터·전처리·시뮬레이션 내용은 두 문제에 공통으로 적용되며, 병원 신설 전용 항목(0-4~0-6, 3-3, 3-6)은 보관용 기록입니다. 구급차 재배치의 규칙·탐색법·결과는 구현이 끝나는 대로 5절에 추가합니다. 전환 배경은 `docs/decisions_and_tradeoffs_ko.md` 13절.

작성 기준: 2026-09-10 KST(초안), 2026-09-11 갱신. 이 문서는 보고서를 쓸 때 참고할 **사실 정리**입니다. 수치는 파일 경로와 함께 적었으니 인용 전에 해당 파일을 다시 확인하십시오. "결과 대기"로 표시된 항목은 실행이 끝나면 갱신됩니다.

관련 문서: 결정과 trade-off는 `docs/decisions_and_tradeoffs_ko.md`, 모델 명세는 `docs/model_spec_ko.md`, 계수 근거는 `docs/calibration_sources_ko.md`, 후보지 정의는 `docs/candidate_sites_ko.md`, 민감도 분석은 `docs/sensitivity_analysis_ko.md`, 시뮬레이터 사용법은 `docs/simulator_ko.md`.

---

## 0. 시뮬레이션 및 연구 규칙

각 규칙을 "무엇을 하지 않는가(X) / 무엇을 하는가(O) / 왜"로 정리했습니다.

### 0-1. 연구 질문과 목적함수

- X: 이동시간 최소화, 커버리지 비율(몇 분 안에 몇 %) 최대화
- O: **신설 병원을 세웠을 때 환자별 기대 생존확률의 합(기대 생존자 수)이 기존 병원만 있을 때보다 얼마나 늘어나는가**를 최대화
- 왜: 시간 단축은 수단이고 목적은 생존입니다. 환자 유형마다 시간 민감도가 다르므로(심정지 8분 vs 경증 60분) 분 단위 지표는 유형 간 가중을 숨깁니다. 생존함수를 쓰면 지연이 곧 손실로 계산되어 별도 벌점이 필요 없습니다.

### 0-2. 분석 단위

- X: 경기도 전체를 하나의 네트워크로 최적화
- O: **시·군 하나씩 독립 문제**로 풀고, 31개 결과 합계는 보고용으로만 계산
- 왜: 병원 신설은 시·군 단위 정책이고, 문제가 작아야 후보 조합 전수탐색이 가능합니다. 대가로 시·군 경계를 넘는 실제 이송(과천→서울 등)은 모델에 없습니다(한계에 명시).

### 0-3. 데이터 원칙 ("fail loud")

- X: 직선거리, 시·군 중심점, 대표 병원 1개, 임의 수치로 빈칸 채우기
- O: 실제 좌표 + 라우팅 서비스(Kakao Mobility)의 방향별 도로 이동시간(분). 자료가 하나라도 없으면 프로그램이 **실패**하고 결과를 만들지 않음
- 왜: 합성값이 섞이면 결과가 실데이터인지 알 수 없게 됩니다. 검증 코드는 `ambulance_sim/hospital_placement.py`의 `validate_municipal_placement_problem`이며, 시·군 코드 접두어가 다른 자원, 출처 누락, 방향별 경로 누락을 모두 예외로 처리합니다.
- 예외: 후보지 이격 판정(2km)과 도로접근 사전점검의 "가장 가까운 시설 고르기"에만 대원거리(Haversine)를 씁니다. 이동시간에는 절대 쓰지 않습니다.

### 0-4. 신설 후보지 정의 (2026-09-10 확정)

- X: 기존 보건소 위치를 후보로 쓰기(이전 세션 방식)
- O: 시·군 위 **1km 격자** 칸 중심. 조건: (1) Kakao 좌표→행정구역 API로 시·군 내부 확인, (2) 칸 추정 인구 > 0, (3) **인구 상위 30% 칸 제외**(NIMBY 고려), (4) 기존 응급의료기관·보건시설에서 **2km 이상**, (5) Kakao가 경로를 찾을 수 있는 칸(도로 접근 가능)
- 왜: "지금 없는 곳에 세운다"는 연구 질문에 맞추기 위해. 상위 인구 칸 제외는 고밀도 주거지 반대(NIMBY)를 반영한 사용자 판단입니다.
- 수원 결과: 154칸 → 시 내부 108 → 상위 30% 제외 74 → 2km 이격 33 → 도로접근 가능 **31곳**. 파일: `data/processed/gyeonggi_20260909/41110/candidate_sites.csv`, 단계별 개수는 `grid_candidates_manifest.json`.

### 0-5. 신설 병원 사양

- O: 모든 후보는 **지역응급의료기관, 응급실 20병상, 병상 점유 75분, 비용 1**로 동일 (수원 기존 7곳 응급실 병상 중앙값 20~21에 맞춤)
- 왜: 위치 효과만 비교하기 위해. 규모 효과는 민감도 스윕(`candidate_capacity`)으로 따로 봅니다.

### 0-6. 치료 성공률 (2026-09-10 확정)

- O: 병원 도착 시 응급실 병상이 있으면 **1.0**, 없으면 **0.1**(진료 거부·대기로 간주). 병원 등급·환자 유형에 따른 차이 없음
- 왜: 119로 이송될 정도면 병상이 필요하다는 판단. 시간 지연은 생존함수에서 이미 반영됨. 대가로 권역센터와 소형 기관의 역량 차이가 사라집니다(한계).

### 0-7. 시간 구조

- 환자 발생: 시간대별 비균질 포아송 과정. 현재는 **평탄**(하루 발생량 ÷ 24). 하루 발생량은 2025년 시·군 실측 출동 건수(수원 210.4건/일). 시간대별 분포는 공식 통계가 확보되면 적용(결과 대기: `docs/calibration_sources_ko.md`).
- episode 길이: 발생 기간 24시간(7일 연속은 시간대 분포가 실데이터일 때만) + **냉각 6시간**. 발생 마감(`arrival_cutoff_minutes`) 이후에는 새 환자가 없고, 이미 발생한 환자만 종결될 때까지 진행. 마감 이후 발생분은 계산하지 않음.
- 도로 혼잡 시간대 배율: **적용하지 않음**. Kakao 수집 시점(2026-09-10)의 1회 스냅샷.
- 계절성·이벤트 날짜(설·추석·핼러윈): **제외**. 1km 격자·시·군 해상도로는 재현 불가. 월별 계절성 자료(`data/processed/calibration/nfa_gyeonggi_monthly_seasonality_2020_2023.csv`)는 후속 과제용으로만 보관.
- 최대 전원 2회. 병원 수용 판정은 도착 시각의 병상 상태.

### 0-8. 통계 규칙

- 후보 조합은 **전수탐색**(`itertools.combinations`), 안전 상한 `--max-combinations`(기본 10,000).
- 모든 조합과 기준안은 **같은 seed 집합**으로 실행(짝비교). seed는 `--seed`부터 episode 수만큼 연속 정수.
- 보고 지표: 기대 생존자 평균, 표본표준편차, 95% 정규근사 신뢰구간, 기준안 대비 증분. episode가 2개 미만이면 신뢰구간은 `null`.
- 시·군 합산 시 분산은 독립 가정으로 합산(`sqrt(Σ sd²)`).
- 결과 해석 규칙: 계수가 "provisional"인 동안은 **절대값 인용 금지, 후보 간 순위만** 사용. 민감도 분석으로 순위의 강건성을 확인.

### 0-9. 수치 정밀도

- X: 특별한 고정밀(fp64 강제) 처리 없음
- O: Python 기본 float(IEEE 754 배정밀도, 사실상 fp64). 생존확률 질량은 `Saved + Lost = 1` 불변식으로 검증. 도로시간 캐시는 소수점 15자리 수준 반올림 차이만 있음(`tests/test_road_network_cache.py`).

### 0-10. 재현성

- 난수는 `random.Random(seed)` 하나로 통제. 같은 입력·같은 seed면 결과가 비트 단위로 같음.
- 모든 계수는 한 파일(`data/processed/calibration/model_parameters_<status>.json`)에서 읽고, `placement.json`의 `provenance.model_parameters`에 어떤 파일·상태를 썼는지 기록.

---

## 1. 데이터 다운로드와 기법

### 1-1. 사용한 공식 자료 (공식 데이터셋 명칭 그대로)

| 공식 데이터셋 명칭 | 제공 기관 | 기준일 | 용도 | 공식 페이지 | 저장 위치 |
|---|---|---|---|---|---|
| 경기도 응급의료기관 현황 | 경기도 (공공데이터포털 15011313 / 경기데이터드림) | 2025-09-17 | 기존 응급의료기관 73곳 위치·등급 | https://www.data.go.kr/data/15011313/fileData.do | `data/raw/public_sources_20260908_203053KST/` |
| 소방청 전국 소방서 및 119안전센터별 구급차 정보 | 소방청 (공공데이터포털 15113282) | 2025-06-30 | 119 거점 232행, 구급차 290대 | https://www.data.go.kr/data/15113282/fileData.do | 같은 폴더 |
| 소방청 119안전센터 현황 | 소방청 (공공데이터포털 15065056) | 2023-12-31 | 거점 주소 보강 | https://www.data.go.kr/data/15065056/fileData.do | 같은 폴더 |
| 행정안전부 주민등록 인구 및 세대현황 전체 읍면동 | 행정안전부 | 2026-08-31 | 행정동 604개 인구 → 수요 배분, 격자 인구 | https://jumin.mois.go.kr/statMonth.do | 같은 폴더 |
| 법정동 코드 전체자료 | 행정안전부 행정표준코드관리시스템 | 2026-09-08 | 코드 매핑 | https://www.code.go.kr/stdcode/regCodeL.do | 같은 폴더 |
| 전국 병의원 및 약국 현황 2026.6 | 건강보험심사평가원(HIRA) | 2026-06 | 응급실 병상 수(정적 신고값) | https://opendata.hira.or.kr/op/opc/selectOpenData.do?sno=11925 | `data/raw/official_hospital_capacity_20260910/` |
| 2024년도 응급의료기관 평가 | 국립중앙의료원 중앙응급의료센터(E-Gen) | 2024 | 기관 코드·평가 등급 보존 | https://www.e-gen.or.kr/nemc/business_medical_institution_evaluation.do?tabId=9 | 같은 폴더 |
| 보건복지부_전국 지역보건의료기관 현황_20251231 | 보건복지부 (공공데이터포털 3072692) | 2025-12-31 | 보건시설 위치(후보 이격 기준, 30개 시·군 후보) | https://www.data.go.kr/data/3072692/fileData.do | `data/raw/official_public_health_facilities_national_20260910/` |
| 소방청_본부별 구급활동정보_20231231, 소방청_본부별 구급활동정보_20201231 | 소방청 (공공데이터포털 15080046) | 2023-12-31, 2020-12-31 | 월별 계절성(후속 과제), 초기 수요 총량 추정 | https://www.data.go.kr/data/15080046/fileData.do | `data/raw/nfa_headquarters_ambulance_activity_20260910/` |
| 구급활동 현황 (경기데이터드림, Sheet 조회, 2025년 출동 1건 1행) | 경기도소방재난본부 / 경기데이터드림 | 2025 | 시·군별 연간 출동 건수 → 일일 수요(수원 76,784건, 210.4건/일), 수원 환자증상·발생장소·연령 분포 | https://data.gg.go.kr/portal/data/service/selectServicePage.do?infId=SE00GA6F273B8PIJ9N8412495661&infSeq=1 | `data/raw/municipal_dispatch_counts_20260910/`, `data/processed/calibration/municipal_daily_calls_2025.csv`, `suwon_patient_category_shares_2025.csv` |
| 소방청 2025년도 119구급서비스 통계연보(최종) (2024.12.31. 기준) | 소방청 | 2024-12-31 | 경기 2024년 출동 799,302건 교차검증 | https://www.nfa.go.kr/nfa/releaseinformation/statisticalinformation/main/?boardId=bbs_0000000000000019&mode=view&cntId=71 | `data/raw/municipal_dispatch_counts_20260910/` |
| Kakao Local API (주소 검색, 좌표→행정구역 변환) | 카카오 | 조회 시점 2026-09-09~10 | 지오코딩, 격자 칸 시·군·행정동 판정 | https://developers.kakao.com/docs/latest/ko/local/dev-guide | 각 CSV의 좌표 열, `grid_candidates_manifest.json` |
| Kakao Mobility 자동차 길찾기 API (Directions, priority=RECOMMEND) | 카카오모빌리티 | 조회 시점 2026-09-10 | 방향별 도로 이동시간(분) | https://developers.kakaomobility.com/docs/navi-api/directions/ | `data/processed/gyeonggi_20260909/<code>/road_times.csv` |

참고용으로만 보관(현재 미사용): 전국보건기관표준데이터(공공데이터포털 15107750, 9개 시·군 58행), 경기도 안산시 보건지소등 현황·경기도 구리시 공공보건기관현황·경기도 양주시 의료시설 현황·경기도 여주시 생활지도(개별 시·군 파일, MOHW 전국 자료로 대체).

임상 계수의 문헌·통계 출처는 `docs/calibration_sources_ko.md`, 환자 분류 체계 조사는 `docs/patient_types_in_practice_ko.md`에 있습니다.

각 폴더의 `source_manifest.json` / `manifest.json`에 URL, 다운로드 시각, SHA-256, 행 수가 기록되어 있습니다. 실패·차단된 출처는 `blocked_sources.json`과 `download_probe_report.md`에 남겼습니다(예: 소방청 2024년 구급활동 파일은 공식 절차로도 0바이트 응답).

### 1-2. 다운로드 기법

- 공공데이터포털 파일은 직접 URL로 받아 SHA-256을 매니페스트에 기록(무결성·재현성).
- 실패한 시도도 기록으로 남김(HTTP 상태, 이유). 보고서에서 "확보하지 못한 자료"를 정직하게 쓸 근거가 됩니다.
- API 키는 `.env`에만 두고 코드·로그·문서에 값을 절대 남기지 않음(`.gitignore`가 `.env` 제외). 로그 출력 시 키 문자열을 `***`로 치환.
- Kakao API는 초당 요청 수를 제한(`--requests-per-second 5`)하고, 일일 쿼터 가드(`--max-calls`)와 재개 기능(이미 수집한 쌍은 건너뜀)을 둠. 경로 탐색 실패(도로 없음)는 별도 파일(`road_times_unroutable.csv`)에 기록하고 계속 진행.
- 시·군별 실제 119 출동 건수: 경기데이터드림 Sheet 조회 경로(`searchSheetData.do`, 시군코드 필터)로 로그인·인증키 없이 2025년 811,214행을 받았습니다. 원본 시군코드 오류(남양주 행이 양주 코드)를 발견해 출동소방서명으로 시·군을 배정했습니다. 원본에는 신고 시각·좌표가 없고 `환자증상1`, `구급처종명`, 연령·성별만 있습니다. 기록: `docs/demand_data_acquisition_ko.md`.
- Kakao 쿼터(공식 확인): 자동차 길찾기 무료 일 10,000건, 좌표 변환 일 100,000건, 초과 시 HTTP 429 또는 400(code -10), 매일 자정 초기화(운영자 답변). 시·군 하나에 5~6천 호출이 필요해 하루 1~2개 시·군 페이스.

---

## 2. 데이터 전처리 기법

### 2-1. 파이프라인 순서 (스크립트 = `scripts/`)

1. `prepare_gyeonggi_inputs.py`: 원본 5종 → 31개 시·군 폴더(수요 604, 병원 73, 구급차 232행/290대). 시·군 코드로 분할, `readiness.json` 생성.
2. `geocode_gyeonggi_kakao.py`: 주소 → 좌표(Kakao Local). 수요지는 **행정복지센터 대표점**(실제 신고 좌표가 아님). `apply_demand_coordinate_overrides.py`: 동명 오검색 3곳(과천 부림동, 의왕 부곡동, 남양주 양정동)을 공식 주소로 교정.
3. `build_hospital_capacity_match.py` → `stage_hospital_capacity.py`: HIRA 병상·E-Gen 평가를 병원 73행에 결합(이름 정규화 + 주소 유사도 매칭, 1행 수동 검토 표시).
4. `prepare_mohw_public_health_facilities.py` → `stage_mohw_candidates.py`: 보건복지부 보건시설 336곳을 시·군별로 스테이징(2026-09-10 이후 수원은 격자 후보로 대체, 보건소 4곳은 `candidate_sites_public_health_reference.csv`로 이관).
5. `build_grid_candidates.py`: 1km 격자 후보 생성(0-4 조건). `probe_candidate_routability.py`: 도로 접근 불가 칸 제외.
6. `collect_kakao_routes.py --pair-set simulation`: 필수 방향쌍 수집. 필수쌍 = (거점∪시설)→수요 + 수요→시설 + 시설→(시설∪거점). 수원 31후보 기준 5,652쌍.
7. `prepare_nfa_monthly_profile.py`: 월별 계절성(후속 과제용).
8. `refresh_gyeonggi_readiness.py --parameters ...`: 준비도 갱신. 차단 항목(blocking)과 권고 항목(advisory) 분리.
9. `build_municipal_placement_inputs.py`: 최적화 입력 `data/gyeonggi_real/<code>_<slug>/{scenario,placement}.json` 생성. 로더 검증을 통과해야 저장됨.

### 2-2. 핵심 전처리 판단

- **수요 배분**: 시·군 일일 발생량 = 2025년 실측 출동 건수 ÷ 365 (수원 76,784건 → 210.4건/일; `model_parameters_*.json`의 `demand.daily_calls_by_municipality`). 행정동별로는 주민등록 인구 비율로 나누고, 시간당 발생률 = 일일량 ÷ 24. 이전의 인구비례 추정(경기도 2023년 846,564건 × 인구비, 수원 199.5건)은 5% 낮았음.
- **격자 인구**: 칸이 속한 행정동(Kakao 행정동 코드) 인구를 그 동에 속한 칸 수로 균등 배분한 추정치. 동일값(tie)이 자주 생겨 상위 30% 절단이 34칸을 제외함(33 예정).
- **노드 ID 규칙**: `41110::demand::4111156000`, `41110::ambulance_base::001`, `41110::hospital::001`, `41110::candidate::GRID-1KM-41110-003-007`. 접두어가 시·군 코드와 다르면 검증 실패.
- **좌표 순서**: `network.positions`는 `[경도, 위도]`(WGS84). `placement.json`의 `provenance.coordinate_order`에 명시.
- **병상**: HIRA 2026-06 정적 신고 병상 = 모델의 `capacity`. 실시간 가용 병상이 아님(`capacity_semantics` 열에 기록).
- **후보 품질 표시**: 모든 후보는 `land_feasibility_status = requires_planning_review_*`. 토지 소유·용도지역·면적 미검증.

---

## 3. 시뮬레이션 제작법

### 3-1. 모델 형태

이벤트 기반 **SMDP(semi-Markov decision process)**. 고정 시간 간격이 아니라 사건(환자 발생, 현장 도착, 처치 완료, 병원 도착, 전원 준비, 병원 방출, 재정비 완료, 재배치 완료, 사후 이송 도착) 단위로 시간이 흐릅니다. 구현: `ambulance_sim/engine.py`(우선순위 큐 `heapq`), 상태 모델 `model.py`, 정책 `policy.py`.

### 3-1a. 실제 사건 재생이 아니라 확률 시뮬레이션이라는 점

이 시뮬레이션은 실제 119 신고 사건을 재생하지 않습니다. 실측 통계로 보정한 확률 모형에서 환자를 무작위로 생성하는 몬테카를로 시뮬레이션입니다.

| 요소 | 실데이터인 부분 | 모형이 만드는 부분 |
|---|---|---|
| 환자 수 | 2025년 수원 실제 출동 76,784건 → 하루 210.4건 | 하루하루의 건수는 포아송 난수 |
| 발생 위치 | 44개 행정동의 주민등록 인구 비율 | 행정동 안에서는 행정복지센터 대표점 한 곳. 실제 신고 좌표 아님 |
| 발생 시각 | 시간대 배율의 공식 앵커 2점(09시 최대·04시 최소) | 나머지 22시간은 보간, 개별 시각은 난수 |
| 환자 유형 | 비율만 통계에서 가져옴 | 개별 환자 유형은 난수 |
| 병원·구급차·도로 | 전부 실제(위치, 병상, 대수, Kakao 도로시간) | 없음 |

사건 단위 실데이터(신고 시각·좌표가 있는 구급활동일지 원자료)는 공개되지 않습니다. 경기데이터드림 자료도 연도·소방서·증상·연령까지만 제공합니다. 보고서에는 "실측 통계로 보정한 확률 시뮬레이션(Monte Carlo)"으로 서술하고, 실제 사건 재생은 연구용 자료 제공 신청이 필요한 후속 과제로 적으십시오.

### 3-1b. 도로접근 불가 후보

격자 칸 중심이 산·저수지·농지에 떨어져 Kakao 길찾기가 "시작 지점 주변의 도로를 탐색할 수 없음"을 반환하면 그 칸은 후보에서 제외합니다(수원 2곳: 권선구 장지동 격자 1-5, 장안구 하광교동 격자 10-8). 도로가 없는 지점은 병원 부지로 성립하지 않는다는 판단이며, 중심점을 도로로 옮기는 대안은 격자 정의를 깨므로 쓰지 않았습니다. 기록: `candidate_sites_unroutable.csv`.

### 3-2. 상태·행동·전이

- 상태: 구급차(위치, 상태, 담당 환자), 환자(발생 노드·시각, 유형, 미해결 생존질량 q), 병원(위치, 병상 점유), 시각.
- 행동(중앙 관제자): 대기 환자에게 가용 구급차 배정 → 현장 처치 후 수용 병원 선택 → 인계 후 재정비 지점 → 재정비 후 대기지 선택.
- 정책(`GreedySurvivalPolicy`): 최근접 유휴 구급차 배정; 병원은 1단계 전원까지 내다본 기대 생존질량이 최대인 곳; 대기지는 수요 커버리지 점수 `Σ rate/(1+이동시간)` 최대 거점.
- 생존함수: `S(t) = 1 (t ≤ 골든타임), exp(-decay·(t-골든타임)) (그 이후)`. 구간 이동 시 `q ← q·S(t1)/S(t0)`로 비율만 적용(중복 감쇠 방지). 병원 성공률 r이면 `Saved += q·r, q ← q·(1-r)`. 모든 종결 환자에 대해 `Saved + Lost = 1`.
- 병원 수용: 도착 시각 병상 유무로 판정. 없으면 성공률 0.1 적용 후 전원(최대 2회).

### 3-3. 입지 최적화 (`hospital_placement.py`)

1. `placement.json`과 `scenario.json`을 읽고 엄격 검증.
2. 기준안(기존 병원만)을 seed 42..42+N-1로 N episode 실행.
3. 후보 k개 조합마다 같은 seed로 실행, episode별 차이(증분)를 기록.
4. 증분 평균이 큰 순으로 정렬. 결과 JSON에 기준안·최선 조합·모든 대안·신뢰구간·출처(provenance) 포함.

명령 예:
```powershell
python scripts/build_municipal_placement_inputs.py --processed-dir data/processed/gyeonggi_20260909 --parameters data/processed/calibration/model_parameters_provisional.json --municipality-code 41110 --horizon-days 1 --cooldown-minutes 360 --overwrite
python -m ambulance_sim --hospital-placement data/gyeonggi_real/41110_suwon/placement.json --new-hospitals 1 --episodes 2 --seed 42 --json --output-dir outputs/suwon_placement_test
python scripts/run_placement_sensitivity.py --placement data/gyeonggi_real/41110_suwon/placement.json --new-hospitals 1 --episodes 2 --seed 42 --output-dir outputs/sensitivity/41110_suwon --seed-sweep 3
python scripts/build_folium_municipal_simulator.py --placement data/gyeonggi_real/41110_suwon/placement.json --processed-dir data/processed/gyeonggi_20260909 --results outputs/suwon_placement_test/hospital_placement_result.json --scenarios baseline,best --seed 42 --output outputs/suwon_folium_simulator.html
```

### 3-4. 성능

- 도로시간은 출발지별 최단경로를 1회 계산해 캐시(`RoadNetwork._shortest_paths_from`). 1 episode 22.6초 → 0.9초(수원, 24시간). 결과는 캐시 전과 소수점까지 동일(기대 생존 130.179526 재현).
- 예상 실행 시간(수원, 31후보, 24h+6h): k=1은 (1+31)×N episode, k=2는 (1+465)×N. N=2면 각각 약 1분, 15분. N=100이면 k=2는 약 13시간 → k=2 최종 실행은 후보를 상위권으로 좁히거나 N을 줄여야 함.

### 3-5. 검증

- 단위 테스트 132개(`python -m pytest -q`): 엔진 불변식, 정책, 입출력 검증, 입지 최적화, 파이프라인 스크립트, 격자 후보, 민감도 도구, 도착 마감, 도로시간 캐시 동치성.
- 합성 예제 결과는 `outputs/synthetic_examples/`로 분리(실제 결과 아님).

### 3-6. 현재 결과 (임시 계수, 참고용)

- 이전 후보 정의(보건소 4곳), 24시간, 100 episode, 임시 계수(성공률 0.86/0.78): 기준안 142.9명(95% CI 140.6~145.2), 최선 후보 영통구보건소 +0.795명(0.748~0.843). 파일: `outputs/archive/suwon_placement_provisional/k1/hospital_placement_result.json`. **후보 정의와 성공률 규칙이 바뀌어 보고서에 쓰지 말 것.**
- 격자 후보 31곳 결과: 결과 대기.

---

## 4. 추가로 알아야 할 것

### 4-1. 보고서에 반드시 써야 할 한계

1. 수요 위치는 행정복지센터 대표점입니다(실제 신고 좌표 아님). 수요량은 시·군 단위 실측(2025년)이지만 행정동 배분은 인구비례 추정입니다.
2. 환자 유형 비율과 생존함수 계수는 조사 중이며(`docs/calibration_sources_ko.md`, `docs/patient_types_in_practice_ko.md`), 확정 전 절대값은 인용 불가입니다.
3. 성공률 1.0/0.1 규칙은 사용자 판단이며 병원 등급 차이를 무시합니다.
4. 시·군 경계 밖 병원 이용을 모델하지 않습니다.
5. 도로시간은 일반 승용차 기준 1회 스냅샷입니다(긴급차량 우선통행, 혼잡 시간대 미반영).
6. 격자 칸 인구는 동 내부 균등 배분 추정입니다.
7. 후보지의 토지·용도·면적은 검증되지 않았습니다.
8. 24시 이후 냉각 6시간 동안 새 수요가 없어 마감 직전 환자가 약간 유리합니다.

### 4-2. 이전 AI 세션이 남긴 것 중 고친 것

- README가 가리키던 입력 파일이 실제로 없었음 → 생성기 구현으로 해결.
- 준비도(`readiness.json`)가 구조적으로 항상 미완료였음 → 조건부로 수정.
- 합성 결과가 실제 결과 폴더에 섞여 있었음 → `outputs/synthetic_examples/`로 분리.
- 배정 루프 조기 종료, 코드 생성 시나리오 검증 누락, episode 1개일 때 폭 0 신뢰구간 등 코드 결함 수정.
- 후보지가 보건소 위치였음 → 격자 후보로 교체.
- 좌표 라벨 변경으로 실패하던 테스트 정정.

### 4-3. 아직 남은 일

- 시간대별 출동 분포·환자 유형·현장처치 시간의 공식 통계 확인(진행 중).
- 수원 `환자증상1` 분포 집계로 환자 유형 비율의 실측 근거 확보(진행 중).
- 나머지 30개 시·군의 격자 후보 생성과 도로시간 수집(Kakao 쿼터에 따라 수일 소요 가능).
- 최종 episode 수 결정과 k=2 실행 전략.
- 브라우저에서 시뮬레이터 시각 확인.

### 4-4. 결과를 읽는 법

- `incremental_expected_saved.mean`이 양수이고 95% CI 하한이 0보다 크면 "그 위치가 기존 대비 기대 생존자를 유의하게 늘린다"고 말할 수 있습니다(정규근사).
- 상위 후보들의 CI가 겹치면 "동률 후보군"으로 묶어 제시하십시오. 민감도 분석의 `robustness.fraction`(기준 최적 조합이 1위를 유지한 비율)을 함께 제시하면 결론의 강건성을 보일 수 있습니다.
- 증분의 절대 크기(예: +0.8명/일)는 계수에 민감합니다. "연간 약 290명" 식 환산은 계수가 확정된 뒤에만 하십시오.


---

## 5. 구급차 상시대기 재배치 (전주시, 현재 연구 문제)

### 5-1. 연구 질문

전주시 119 구급차 12대(안전센터 9~10곳 소속)의 **주간 상시대기 위치를 바꾸면** 하루 기대 생존자가 얼마나 늘어나는가. 기준안은 "모든 구급차가 자기 안전센터에서 대기"이고, 대안은 "일부 구급차를 격자 대기지로 이동"입니다. 신축 없이 운영 변경만으로 얻는 효과를 정량화합니다.

### 5-2. 규칙 (사용자 결정, `docs/decisions_and_tradeoffs_ko.md` 13~17절)

- 07:00~24:00(00:00~01:00 포함): 소속과 무관하게 배정된 대기지에서 대기.
- 01:00~06:00: 원소속 안전센터로 복귀하는 목표. 우선순위 최하위.
- 우선순위: 환자 배정 > 병원 선택 > 재정비 > 대기지 이동·복귀. 환자를 처리 중인 차량은 교대 시각에도 건드리지 않음. 이동 중(repositioning)인 차량은 현재 엔진에서 출동 대상이 아니며, 그 가용성 손실은 결과에 그대로 반영됨.
- 대기지 후보: 시 내부 1km 격자 칸(인구 > 0, Kakao 경로 탐색 가능) 중 119안전센터 1km 이내 제외 + 기존 안전센터 10곳. 인구 상위 제외 없음. 전주: 132칸 → 시 내부 119 → 이격 92 → 도로접근 92 전부 통과 → 대기지 102곳(격자 92 + 안전센터 10).
- 목적함수·시간 구조는 병원 문제와 동일: 기대 생존자 최대화, 24h 발생 + 6h 냉각, 시간대 배율은 보정 파일 값(공식 앵커 2점 + 보간), 도로 혼잡 배율 없음.

### 5-3. 전주 입력 (공식 자료 명칭, `docs/jeonju_data_ko.md`)

| 항목 | 값 | 출처 |
|---|---|---|
| 행정동 수요지 | 35곳(완산 19, 덕진 16), 인구 618,908명 | 행정안전부 주민등록 인구 및 세대현황 전체 읍면동 (2026-08) |
| 119안전센터 / 구급차 | 10행(9개소) / 12대 | 소방청 전국 소방서 및 119안전센터별 구급차 정보 (2025-06-30) |
| 응급의료기관 | 5곳: 전북대학교병원(권역), 예수병원·대자인병원·전주병원(지역센터), 호성전주병원(지역기관). 응급실 병상 합 164 | 국립중앙의료원 2024년도 응급의료기관 평가, 건강보험심사평가원 전국 병의원 및 약국 현황 2026.6 |
| 일일 출동 | 104.33건(2024년 38,183건, 안전센터 9개소 합산 **실측**) | 소방청 2025년도 119구급서비스 통계연보(최종) 부록 |
| 도로시간 | 6,780 방향쌍(운영 770 + 대기지 6,010), 실패 0 | Kakao Mobility 자동차 길찾기 API (RECOMMEND), 2026-09-11 10:35~10:52 수집 |
| 좌표 | 수요 35/35, 거점 10/10, 병원 5/5 | Kakao Local 주소·키워드 검색, 좌표→행정구역 변환 |

인구비례 추정을 썼다면 54,540건(+42.8%)으로 크게 빗나갔을 값입니다. 환자 유형 구성비는 전북 출동 원자료가 없어 보정 파일의 전국 통계 값을 씁니다.

### 5-4. 탐색 방법 (`ambulance_sim/standby_placement.py`)

12대 × 102곳 배정은 전수탐색이 불가능하므로 **탐욕 순차 배정**을 씁니다.

1. 기준안(전원 원소속)을 seed 42, 43, …로 N episode 실행.
2. 후보 사전선별: 수요 커버리지 점수 Σ(시간평균 발생률 ÷ (1 + 이동시간)) 상위 K개 대기지 + 모든 안전센터.
3. 매 단계에서 (구급차, 대기지) 조합을 전부 같은 seed로 평가해 기대 생존자 증분 평균이 가장 큰 이동 하나를 채택. 이미 다른 차량이 쓰는 대기지는 제외.
4. 증분이 0 이하이거나 이동 대수 상한(`--movable`)에 닿으면 종료. 모든 단계를 기록.

전역 최적을 보장하지 않으므로 보고서에 "탐욕 해"임을 명시하고, seed와 K를 바꿔 같은 배정이 나오는지 확인합니다.

### 5-5. 결과

- 기준안 시뮬레이터: `outputs/jeonju_standby_simulator_baseline.html` (seed 42 하루: 환자 91명, 기대 생존 79.48, 손실 11.52).
- 2 episode 시험 최적화(`outputs/archive/jeonju_standby_test/`): 결과 대기.
- 예비 실행(episode 수 미확정): 결과 대기.

### 5-6. 이 문제에 특유한 한계

1. 격자 칸 중심에 실제로 구급차가 대기할 공간(주차·휴식·화장실)이 있는지 검증하지 않았습니다.
2. 소방서 관할 경계, 교대 근무, 2인 1조 근무 규정 등 운영 제약이 모델에 없습니다.
3. 재배치 이동의 연료·인력 비용은 목적함수에 없습니다.
4. 시간대별 수요 배율의 22개 값이 보간이라 야간 복귀 규칙의 효과가 이 배율에 좌우됩니다.
5. 도로시간은 수집 시점 1회 스냅샷입니다. 같은 구간을 20분 간격으로 두 번 조회했을 때 수초 차이가 났습니다(결정 기록 18절).
6. 조촌119안전센터는 2024년 연보에 없어(2025 신설 추정) 출동 건수는 11대 체제 값입니다.
